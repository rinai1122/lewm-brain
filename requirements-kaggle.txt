# NOTE: not consumed by the Kaggle notebook recipe anymore — Cell 1
# installs allensdk directly from its master branch because the PyPI
# release (2.16.2, Nov 2023) pins numpy<1.24 and has no Python 3.12
# wheel for that numpy. Kept here for documentation / future use.
#
# When a post-2.16.2 allensdk is released to PyPI (with Python 3.12
# support), revert Cell 1 to `pip install allensdk` and pin here.
git+https://github.com/AllenInstitute/AllenSDK.git
