# Pure additions on top of the Kaggle base image. The base image already
# ships torch, transformers, numpy, pandas, scikit-learn, xarray,
# matplotlib at recent versions. Allen is the only thing missing.
#
# We deliberately do NOT pin pynwb / hdmf / h5py here — let allensdk's
# own dep solver pick versions that match its release.
allensdk
